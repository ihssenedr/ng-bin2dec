import { BinaryValidatorDirective } from './binary-validator.directive';

describe('BinaryValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new BinaryValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
